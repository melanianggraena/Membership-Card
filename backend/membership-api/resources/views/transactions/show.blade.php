@extends('layouts.app') @section('title','Detail Transaksi') @section('content')
<a class="back-link" href="{{ route('transactions.index') }}"><i data-lucide="arrow-left"></i>Kembali ke Transaksi</a><div class="page-head"><div><h1>Detail Transaksi</h1><p>{{ $transaction->transaction_code }}</p></div><span class="badge large {{ $transaction->status==='success'?'success':'danger' }}">{{ $transaction->status==='success'?'Berhasil':'Gagal' }}</span></div>
<section class="receipt-card"><div class="receipt-head"><span class="stat-icon rose"><i data-lucide="receipt-text"></i></span><div><small>Kode Transaksi</small><h2>{{ $transaction->transaction_code }}</h2></div></div><dl class="detail-grid"><div><dt>Member</dt><dd><b>{{ $transaction->member?->full_name ?? '-' }}</b><small>{{ $transaction->member?->member_code }}</small></dd></div><div><dt>Jenis</dt><dd>{{ ['top_up'=>'Top Up','room_access'=>'Akses Ruangan','outlet_purchase'=>'Pembelian Outlet'][$transaction->transaction_type] ?? $transaction->transaction_type }}</dd></div><div><dt>Outlet / Ruangan</dt><dd>{{ $transaction->outlet?->outlet_name ?? $transaction->room?->room_name ?? '-' }}</dd></div><div><dt>Nominal</dt><dd><b class="amount">Rp {{ number_format($transaction->amount,0,',','.') }}</b></dd></div><div><dt>Saldo Sebelum</dt><dd>Rp {{ number_format($transaction->balance_before,0,',','.') }}</dd></div><div><dt>Saldo Sesudah</dt><dd>Rp {{ number_format($transaction->balance_after,0,',','.') }}</dd></div><div><dt>Diproses Oleh</dt><dd>{{ $transaction->admin?->name ?? 'Sistem' }}<small>{{ ucfirst($transaction->admin?->role ?? '') }}</small></dd></div><div><dt>Tanggal</dt><dd>{{ $transaction->created_at->translatedFormat('d F Y') }}<small>{{ $transaction->created_at->format('H:i:s') }} WIB</small></dd></div></dl></section>@endsection
