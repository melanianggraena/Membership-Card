@extends('layouts.app') @section('title','Manajemen Outlet') @section('content')
<div class="page-head"><div><h1>Manajemen Outlet</h1><p>Kelola ketersediaan, kapasitas, dan harga outlet.</p></div>@if(auth()->user()->role==='admin')<button class="btn btn-primary" data-modal-open="roomModal"><i data-lucide="plus-circle"></i>Tambah Outlet</button>@endif</div>
<div class="stats-grid three"><article class="stat-card"><small>Total Outlet</small><strong>{{ $rooms->total() }}</strong></article><article class="stat-card"><small>Outlet Aktif</small><strong>{{ \App\Models\Room::where('status','active')->count() }}</strong></article><article class="stat-card"><small>Outlet Nonaktif</small><strong>{{ \App\Models\Room::where('status','inactive')->count() }}</strong></article></div>
<section class="card"><div class="table-wrap"><table><thead><tr><th>Nama Outlet</th><th>Deskripsi</th><th>Harga Akses</th><th>Kapasitas</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($rooms as $room)<tr><td><b>{{ $room->room_name }}</b></td><td>{{ Str::limit($room->description,45) ?: '-' }}</td><td>Rp {{ number_format($room->access_price,0,',','.') }}</td><td>{{ $room->capacity }} orang</td><td><span class="badge {{ $room->status==='active'?'success':'danger' }}">{{ $room->status==='active'?'Aktif':'Nonaktif' }}</span></td><td>@if(auth()->user()->role==='admin')<button class="icon-btn" data-room='@json($room)' data-modal-open="roomModal"><i data-lucide="pencil"></i></button>@endif</td></tr>@empty<tr><td colspan="6" class="empty">Belum ada outlet.</td></tr>@endforelse</tbody></table></div><div class="pagination-wrap">{{ $rooms->links() }}</div></section>@endsection
@push('modals')<div class="modal" id="roomModal"><div class="modal-card"><div class="card-head"><h2 data-room-title>Tambah Outlet</h2><button class="icon-btn" data-modal-close><i data-lucide="x"></i></button></div><form method="POST" action="{{ route('rooms.store') }}" data-room-form>@csrf<div data-method></div><label>Nama Outlet<input name="room_name" required></label><label>Deskripsi<textarea name="description" rows="3"></textarea></label><div class="form-grid"><label>Harga Akses<input name="access_price" type="number" min="0" required></label><label>Kapasitas<input name="capacity" type="number" min="1" required></label><label>Status<select name="status"><option value="active">Aktif</option><option value="inactive">Nonaktif</option></select></label></div><div class="form-actions"><button type="button" class="btn btn-secondary" data-modal-close>Batal</button><button class="btn btn-primary">Simpan</button></div></form></div></div>@endpush
