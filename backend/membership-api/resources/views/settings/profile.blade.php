@extends('layouts.app') @section('title','Pengaturan Profil') @section('content')
<div class="page-head"><div><h1>Pengaturan</h1><p>Kelola profil dan preferensi akun Anda.</p></div></div>
<div class="settings-grid">@include('settings.nav')<form class="form-card" method="POST" action="{{ route('settings.profile') }}">@csrf @method('PUT')<h2>Informasi Profil</h2><p class="muted">Perbarui nama dan email akun administrator.</p>@if($errors->any())<div class="alert alert-error">{{ $errors->first() }}</div>@endif<label>Nama<input name="name" value="{{ old('name',auth()->user()->name) }}" required></label><label>Email<input type="email" name="email" value="{{ old('email',auth()->user()->email) }}" required></label><div class="form-actions"><button class="btn btn-primary">Simpan Perubahan</button></div></form></div>@endsection
